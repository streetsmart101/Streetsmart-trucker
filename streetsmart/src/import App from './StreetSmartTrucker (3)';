import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './StreetSmartTrucker (3)';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
